const REFERENCE_STORAGE_KEY='leadintel_customer_v2_state';
const FIRECRAWL_PROXY='https://apollo-proxy.edgars-7e7.workers.dev';
const PDFJS_VERSION='6.2.108';
const XLSX_VERSION='0.18.5';
const MAX_REFERENCE_ANALYSIS=24;
let pdfModule=null;

(function installReferenceCustomerUI(root){
  const Ref=root.LeadIntelReferenceCustomers;if(!Ref||typeof document==='undefined')return;
  const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const clean=value=>String(value??'').replace(/\s+/g,' ').trim();
  let editingTarget=-1;
  function readState(){try{return JSON.parse(localStorage.getItem(REFERENCE_STORAGE_KEY)||'{}');}catch{return {};}}
  function writeState(state){return Ref.persistReferenceWorkspaceState(root,state,{render:false});}
  async function syncTargets(){const bridge=root.LeadIntelServerBridge;if(bridge?.session?.authenticated&&bridge?.workspace){const result=await bridge.saveNow?.({saveIntent:true,explicitSave:true});if(!result?.saved)throw new Error('Target saved in this browser. Use Save workspace to sync it to your account.');}}
  async function writeTargets(state){writeState(state);await syncTargets();}
  function ensureState(state){state.referenceCustomers=Ref.normalizeReferenceState(state.referenceCustomers||{});return state;}
  function toast(message){const node=document.getElementById('toast');if(!node)return;node.textContent=message;node.classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>node.classList.remove('show'),2600);}
  function ensureModal(){
    let modal=document.getElementById('reference-customer-modal');if(modal)return modal;
    modal=document.createElement('div');modal.id='reference-customer-modal';modal.className='reference-customer-modal';modal.hidden=true;
    modal.innerHTML=`<div class="reference-customer-backdrop" data-reference-close></div><section class="reference-customer-dialog" role="dialog" aria-modal="true" aria-labelledby="reference-customer-title">
      <header><div><span class="eyebrow">Company lists</span><h2 id="reference-customer-title">Customers and target companies</h2><p>Keep companies that bought from you separate from companies you want to win.</p></div><button type="button" class="reference-close" data-reference-close aria-label="Close">×</button></header>
      <nav class="reference-segment-tabs" aria-label="Company list type"><button type="button" data-company-segment="customers" aria-current="page">Top Customers</button><button type="button" data-company-segment="targets">Target Companies</button></nav>
      <section class="target-companies-panel" id="target-companies-panel" hidden><h3>Companies you want to approach</h3><p>Add prospects for research in your selected market. LeadIntel scores a company only when it finds qualifying evidence.</p><form id="target-company-form" class="target-company-form"><label>Company name<input name="companyName" required maxlength="180" placeholder="e.g. Södra"></label><label>Website (optional)<input name="website" inputmode="url" placeholder="https://example.com"></label><button class="primary-btn" type="submit">Add target</button><button class="secondary-btn" type="button" id="target-cancel-edit" hidden>Cancel edit</button></form><div id="target-company-status" role="status" aria-live="polite"></div><div id="target-company-list"></div><div class="target-company-actions"><button class="secondary-btn" type="button" id="targets-copy-draft">Copy from Top Customers</button><button class="primary-btn" type="button" id="targets-research">Research target companies now →</button></div><small id="targets-copy-note">Research opens Step 4 and starts a search for evidence about these targets. Copying keeps the original customer list.</small></section>
      <div class="reference-workflow"><span class="active">1 · Upload</span><span>2 · Analyze</span><span>3 · Review</span><span>4 · Activate</span></div>
      <div id="reference-market-summary" class="reference-market-summary"></div>
      <section class="reference-format-guide"><div><strong>Keep the list simple</strong><p><b>Include a Company Name, Website, or both.</b> LeadIntel can find missing information before analysis.</p><div class="reference-format-example"><span>Company Name</span><span>Website</span><span>Example Company</span><span>https://example.com</span></div></div><button class="secondary-btn" type="button" id="reference-template-download">Download example CSV</button></section>
      <div class="reference-import-actions"><button class="primary-btn reference-upload" type="button" id="reference-upload-button" aria-label="Upload &amp; Analyze Customers">Upload &amp; Analyze Customers</button><input type="file" id="reference-file-input" accept=".csv,.xlsx,.xls,text/csv" hidden><button class="secondary-btn" type="button" id="reference-add-manual" aria-controls="reference-manual-form" aria-expanded="false">Add manually</button><details class="reference-pdf-fallback"><summary>Have only a PDF?</summary><button class="text-btn reference-upload-pdf" type="button" id="reference-pdf-upload-button">Upload PDF for review</button><input type="file" id="reference-pdf-input" accept=".pdf,application/pdf" hidden></details></div>
      <form id="reference-manual-form" class="reference-manual-form" aria-label="Add a reference company" hidden><label for="reference-manual-company">Company name<input id="reference-manual-company" name="companyName" placeholder="Company Name" autocomplete="organization"></label><label for="reference-manual-website">Website <span>(optional)</span><input id="reference-manual-website" name="website" placeholder="Website (optional)" inputmode="url" autocomplete="url"></label><div class="reference-manual-actions"><button class="primary-btn small" type="submit" id="reference-save-manual">Add company</button><button class="secondary-btn small" type="button" id="reference-cancel-manual">Cancel</button></div></form>
      <div id="reference-import-status" class="reference-import-status" aria-live="polite"></div>
      <div class="reference-table-wrap"><table class="reference-table"><thead><tr><th>Company</th><th>Website</th><th>Analysis</th><th></th></tr></thead><tbody id="reference-table-body"></tbody></table></div>
      <div class="reference-analysis-actions"><button class="primary-btn" type="button" id="reference-analyze">Analyze customer list</button><small>LeadIntel will scrape the ready websites and create a compact commercial profile for each company. Website content itself is not stored here.</small></div>
      <section id="reference-segment-review" class="reference-segment-review" hidden><div class="reference-section-head"><div><span class="eyebrow">Suggested customer profile</span><h3>Review the profile LeadIntel inferred</h3></div><span id="reference-analysis-count"></span></div><div id="reference-profile-sources" class="reference-profile-sources"></div><div id="reference-segments"></div><div id="customer-opportunity-map"></div><p class="reference-activation-note"><strong>Review before activation.</strong> Only traits repeated across a segment will guide Discovery and company ranking in your Step 1 market. One-off observations stay visible for review and do not enter the lookalike model. This does not exclude other companies or add these references to outreach.</p></section>
      <div id="reference-dna-summary" class="reference-dna-summary"></div>
      <footer><button class="secondary-btn" type="button" data-reference-close>Close</button><button class="primary-btn" type="button" id="reference-activate" disabled>Activate selected segments</button></footer>
    </section>`;
    document.body.appendChild(modal);bindModal(modal);return modal;
  }
  function bindModal(modal){
    modal.addEventListener('click',event=>{
      const companySegment=event.target.closest('[data-company-segment]');if(companySegment){switchSegment(companySegment.dataset.companySegment);return;}
      if(event.target.closest('[data-show-targets]')){switchSegment('targets');return;}
      const targetRemove=event.target.closest('[data-remove-target]');if(targetRemove){removeTarget(targetRemove.dataset.removeTarget);return;}
      const targetEdit=event.target.closest('[data-edit-target]');if(targetEdit){editTarget(targetEdit.dataset.editTarget);return;}
      if(event.target.closest('[data-reference-close]'))close();
      const confirm=event.target.closest('[data-confirm-reference]');if(confirm)confirmRow(confirm.dataset.confirmReference);
      const remove=event.target.closest('[data-remove-reference]');if(remove)removeRow(remove.dataset.removeReference);
      const segment=event.target.closest('[data-reference-segment]');if(segment)setTimeout(updateActivateButton,0);
    });
    const fileInput=modal.querySelector('#reference-file-input');
    modal.querySelector('#reference-upload-button')?.addEventListener('click',()=>{if(!fileInput)return;fileInput.value='';fileInput.click();});
    fileInput?.addEventListener('change',async event=>{try{await handleFile(event.target.files?.[0]);}finally{event.target.value='';}});
    const pdfInput=modal.querySelector('#reference-pdf-input');
    modal.querySelector('#reference-pdf-upload-button')?.addEventListener('click',()=>{if(!pdfInput)return;pdfInput.value='';pdfInput.click();});
    pdfInput?.addEventListener('change',async event=>{try{await handleFile(event.target.files?.[0]);}finally{event.target.value='';}});
    modal.querySelector('#reference-template-download')?.addEventListener('click',downloadTemplate);
    const manualToggle=modal.querySelector('#reference-add-manual'),manualForm=modal.querySelector('#reference-manual-form');
    manualToggle?.addEventListener('click',()=>{
      const opening=Boolean(manualForm?.hidden);if(manualForm)manualForm.hidden=!opening;
      manualToggle.setAttribute('aria-expanded',String(opening));
      if(opening){modal.querySelector('#reference-manual-company')?.focus();modal.querySelector('#reference-import-status').textContent='';}
    });
    manualForm?.addEventListener('submit',event=>{void addManual(event);});
    modal.querySelector('#reference-cancel-manual')?.addEventListener('click',()=>{
      if(manualForm)manualForm.hidden=true;manualToggle?.setAttribute('aria-expanded','false');manualToggle?.focus();
    });
    modal.querySelector('#reference-analyze')?.addEventListener('click',analyzeCustomerList);
    modal.querySelector('#reference-activate')?.addEventListener('click',activateSelectedSegments);
    modal.querySelector('#target-company-form')?.addEventListener('submit',addTarget);
    modal.addEventListener('submit',event=>{if(event.target.id==='customer-opportunity-form')void buildOpportunityMap(event);});
    modal.addEventListener('click',event=>{if(event.target.closest('[data-research-opportunity-map]')){close();root.dispatchEvent(new CustomEvent('leadintel:open-discovery',{detail:{focus:'companies'}}));}});
    modal.querySelector('#target-cancel-edit')?.addEventListener('click',cancelTargetEdit);
    modal.querySelector('#targets-copy-draft')?.addEventListener('click',copyDraftTargets);
    modal.querySelector('#targets-research')?.addEventListener('click',()=>{if(!targets(readState()).length){modal.querySelector('#target-company-status').textContent='Add a target company first.';return;}close();root.dispatchEvent(new CustomEvent('leadintel:open-discovery',{detail:{focus:'companies',source:'target-companies',startResearch:true}}));});
  }
  function switchSegment(value){const modal=ensureModal(),targets=value==='targets';modal.querySelector('.reference-customer-dialog').classList.toggle('show-targets',targets);modal.querySelector('#target-companies-panel').hidden=!targets;for(const button of modal.querySelectorAll('[data-company-segment]')){if(button.dataset.companySegment===(targets?'targets':'customers'))button.setAttribute('aria-current','page');else button.removeAttribute('aria-current');}if(targets)renderTargets();}
  function open(segment='customers'){const modal=ensureModal();modal.hidden=false;switchSegment(segment);render();}
  function close(){const modal=document.getElementById('reference-customer-modal');if(modal)modal.hidden=true;}
  function targets(state){return Ref.normalizeTargetCompanies(state.targetCompanies,state.website||state.profile?.website);}
  function renderTargets(){const modal=ensureModal(),state=ensureState(readState()),items=targets(state),source=state.referenceCustomers.rows;modal.querySelector('#target-company-list').innerHTML=items.length?`<div class="target-list-heading"><strong>Target Companies · ${items.length}</strong><span>Edit or remove a company below.</span></div><table class="reference-table"><thead><tr><th>Company</th><th>Website</th><th>Research</th><th>Actions</th></tr></thead><tbody>${items.map((item,index)=>`<tr><td><strong>${esc(item.companyName)}</strong></td><td>${item.website?`<a href="${esc(item.website)}" target="_blank" rel="noopener noreferrer">${esc(item.domain)}</a>`:'Website needed'}</td><td>Research pending</td><td><button type="button" class="text-btn" data-edit-target="${index}">Edit</button><button type="button" class="text-btn" data-remove-target="${index}">Remove</button></td></tr>`).join('')}</tbody></table>`:'<p>No targets added yet.</p>';const copy=modal.querySelector('#targets-copy-draft'),available=Ref.normalizeTargetCompanies([...items,...source.map(row=>({companyName:row.companyName,website:row.website}))],state.website||state.profile?.website).length-items.length;copy.hidden=!source.length;copy.disabled=available===0;copy.textContent=available?`Copy ${available} new from Top Customers`:'Top Customers already listed';modal.querySelector('#targets-copy-note').hidden=!source.length;}
  function cancelTargetEdit(){editingTarget=-1;const form=ensureModal().querySelector('#target-company-form');form.reset();form.querySelector('[type="submit"]').textContent='Add target';ensureModal().querySelector('#target-cancel-edit').hidden=true;}
  function editTarget(index){const item=targets(readState())[Number(index)];if(!item)return;editingTarget=Number(index);const modal=ensureModal(),form=modal.querySelector('#target-company-form');form.elements.companyName.value=item.companyName;form.elements.website.value=item.website;form.querySelector('[type="submit"]').textContent='Save changes';modal.querySelector('#target-cancel-edit').hidden=false;form.elements.companyName.focus();}
  async function addTarget(event){event.preventDefault();const form=event.currentTarget,companyName=clean(form.elements.companyName.value),rawWebsite=clean(form.elements.website.value),status=ensureModal().querySelector('#target-company-status');if(!companyName)return;const website=rawWebsite?Ref.normalizeUrl(rawWebsite):'';if(rawWebsite&&!website){status.textContent='Enter a valid website or leave it blank.';return;}const state=readState(),existing=targets(state),updating=editingTarget>=0&&editingTarget<existing.length,candidate={companyName,website,addedAt:updating?existing[editingTarget].addedAt:undefined};const proposed=updating?existing.map((item,index)=>index===editingTarget?candidate:item):[...existing,candidate];const next=Ref.normalizeTargetCompanies(proposed,state.website||state.profile?.website);if(next.length!==proposed.length){status.textContent='This company is already listed, is your own company, or the list has reached 50 targets.';return;}state.targetCompanies=next;writeState(state);renderTargets();cancelTargetEdit();status.textContent=`${companyName} ${updating?'updated':'added'} in Target Companies.`;root.dispatchEvent(new CustomEvent('leadintel:target-companies-updated'));try{await syncTargets();}catch(error){status.textContent=error.message||'Target saved in this browser; use Save workspace to sync it.';}renderTargets();}
  async function removeTarget(index){const state=readState(),items=targets(state);items.splice(Number(index),1);state.targetCompanies=items;cancelTargetEdit();writeState(state);renderTargets();root.dispatchEvent(new CustomEvent('leadintel:target-companies-updated'));try{await syncTargets();}catch(error){ensureModal().querySelector('#target-company-status').textContent=error.message||'Use Save workspace to sync this change.';}renderTargets();}
  async function copyDraftTargets(){const state=ensureState(readState()),existing=targets(state),copy=state.referenceCustomers.rows.map(row=>({companyName:row.companyName,website:row.website}));const next=Ref.normalizeTargetCompanies([...existing,...copy],state.website||state.profile?.website),added=next.length-existing.length;const status=ensureModal().querySelector('#target-company-status');if(!added){const names=copy.filter(row=>existing.some(item=>(Ref.domain(row.website)&&item.domain===Ref.domain(row.website))||(!row.website&&item.companyName.toLowerCase()===row.companyName.toLowerCase()))).map(row=>row.companyName);status.textContent=names.length?`No new companies added. ${names.join(', ')} ${names.length===1?'is':'are'} already in Target Companies.`:'No new companies added. These customers are already listed or excluded from Target Companies.';renderTargets();return;}state.targetCompanies=next;try{await writeTargets(state);status.textContent=`Added ${added} new ${added===1?'company':'companies'} to Target Companies. Existing entries were not duplicated. Review Top Customers before activating its customer model.`;}catch(error){status.textContent=error.message||'Targets copied locally; use Save workspace to sync.';}renderTargets();root.dispatchEvent(new CustomEvent('leadintel:target-companies-updated'));}
  function selectedSegmentIds(){return [...document.querySelectorAll('input[data-reference-segment]:checked:not(:disabled)')].map(node=>node.dataset.referenceSegment);}
  function updateActivateButton(){
    const selected=selectedSegmentIds().length>0;
    const button=document.getElementById('reference-activate');if(button)button.disabled=!selected;
    const inline=document.querySelector('[data-activate-reviewed-profile]');if(!inline)return;
    const reference=readState().referenceCustomers||{};
    const analyzed=Object.keys(reference.analyses||{}).length;
    const mapReady=Boolean(reference.opportunityMap?.hypotheses?.length&&reference.opportunityMap.analysisAt===reference.analyzedAt);
    inline.disabled=!selected||(analyzed===1&&!mapReady);
    inline.title=analyzed===1&&!mapReady?'Build the Opportunity Map first':!selected?'Tick a relevant profile above first':'';
  }
  function downloadTemplate(){const csv='Company Name,Website\nExample Company,https://example.com\nWebsite-only example,https://example.org\n';const blob=new Blob([csv],{type:'text/csv;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='LeadIntel_Reference_Customers_Template.csv';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);}
  function render(){
    const modal=ensureModal(),state=ensureState(readState()),reference=state.referenceCustomers,body=modal.querySelector('#reference-table-body');
    modal.querySelector('#reference-market-summary').innerHTML=`<strong>Opportunity market</strong><span>${esc((state.targetMarkets||[]).join(' · ')||'Select your target market in Step 1')}</span><small>LeadIntel searches for companies with relevant needs and public signals in this market. Past customers provide optional buyer context.</small>`;
    body.innerHTML=reference.rows.length?reference.rows.map(row=>{const analysis=reference.analyses?.[row.id];return `<tr><td><strong>${row.companyName?esc(row.companyName):'<span class="reference-warning">Company name missing</span>'}</strong></td><td>${row.website?`<a href="${esc(row.website)}" target="_blank" rel="noopener noreferrer">${esc(row.domain||row.website)}</a>`:'<span class="reference-warning">Website missing</span>'}</td><td>${analysis?`<span class="reference-status reference-status-ready">Analyzed · ${esc(analysis.confidence)}</span>`:`<span class="reference-status reference-status-${esc(row.status)}">${row.status==='needs_review'?'Needs review':row.status==='ready'?'Ready':'Needs website'}</span>`}</td><td>${row.status==='needs_review'?`<button class="text-btn" type="button" data-confirm-reference="${esc(row.id)}">Confirm</button>`:''}<button class="text-btn" type="button" data-remove-reference="${esc(row.id)}">Remove</button></td></tr>`}).join(''):`<tr><td colspan="4" class="reference-empty">Upload a two-column customer list: Company Name + Website.</td></tr>`;
    renderSegments(reference,state);
    renderOpportunityMap(state,reference);
    const dna=reference.dna;modal.querySelector('#reference-dna-summary').innerHTML=dna?`<div class="reference-active-model"><span class="eyebrow">${dna.dimensions?.length?'Past customer buyer context':'No shared customer pattern'}</span><strong>${esc(dna.profileName||'Reference customer model')} · ${dna.sampleSize||dna.activeCount} references · ${esc(dna.profileConfidence||dna.confidence)} confidence</strong>${dna.profileSummary?`<p>${esc(dna.profileSummary)}</p>`:''}<p>${dna.dimensions?.length?`These buyer traits help refine research in ${(state.targetMarkets||[]).join(' · ')||'your selected market'}; verified opportunities rank by fit and demand signals.`:'No repeated traits were found. Opportunity discovery still uses your offers, market and demand signals without this list.'}</p><div>${(dna.dimensions||[]).slice(0,7).map(d=>`<span><b>${esc(d.label||d.key)}</b> ${(d.values||[]).map(value=>`${esc(value)} (${Number(d.evidenceByValue?.[value]||d.evidenceCount)||0}/${dna.sampleSize})`).join(', ')}</span>`).join('')}</div></div>`:'';
    updateActivateButton();
  }
  function renderSegments(reference,state){const section=document.getElementById('reference-segment-review'),container=document.getElementById('reference-segments'),count=document.getElementById('reference-analysis-count'),sources=document.getElementById('reference-profile-sources');if(!section||!container)return;const segments=reference.segments||[];section.hidden=!segments.length;if(!segments.length){container.innerHTML='';if(sources)sources.innerHTML='';return;}const analyzedRows=reference.rows.filter(row=>reference.analyses?.[row.id]);count.textContent=`${analyzedRows.length} analyzed`;if(sources)sources.innerHTML=`<strong>${analyzedRows.length===1?'Analyzed customer':'Customers behind this profile'}</strong><div>${analyzedRows.map(row=>`<span><b>${esc(row.companyName)}</b>${row.website?`<a href="${esc(row.website)}" target="_blank" rel="noopener noreferrer">${esc(row.domain||row.website)} ↗</a>`:'<small>Website unavailable</small>'}</span>`).join('')}</div>`;
    const sampleSize=Object.keys(reference.analyses||{}).length,first=segments[0],noRecurring=segments.length===1&&first.count>1&&!first.recurringDimensionCount;
    const notice=reference.segmentationMeaningful?'':`<div class="reference-coherent-note"><strong>${noRecurring?'No shared customer pattern detected':first.confidence==='low'?'Early buyer hypothesis':first.confidence==='medium'?'Provisional customer profile':'Shared customer profile suggested'}</strong><span>${noRecurring?`These ${sampleSize} companies have no repeated customer traits. They cannot calibrate buyer targeting, but opportunity discovery still works from your offers, market and buying signals. Use only companies that actually bought from you.`:`Inferred from ${sampleSize} actual customer${sampleSize===1?'':'s'}. With one example, treat the traits as a low-confidence hypothesis. Opportunity ranking still follows verified fit and demand signals.`}</span></div>`;
    const listId=state.referenceCustomerPortfolio?.selectedListId;
    const active=state.referenceCustomerPortfolio?.lists?.some(list=>list.id===listId&&list.active);
    container.innerHTML=notice+segments.map(segment=>{const disabled=segment.canActivate===false,checked=!disabled&&reference.activeSegmentIds?.includes(segment.id),observationLabel=segment.recurringDimensionCount?'Observed attributes':'One-off attributes · not used in Discovery';return `<label class="reference-segment-card${disabled?' reference-segment-card-disabled':''}"><input type="checkbox" data-reference-segment="${esc(segment.id)}" aria-label="Select ${esc(segment.name)} for the customer model" ${checked?'checked':''} ${disabled?'disabled':''}><div><div class="reference-segment-head"><strong>${esc(segment.name)}</strong><span>${segment.count} companies · ${esc(segment.confidence)} confidence</span></div><b class="reference-select-hint">${disabled?'This profile cannot be activated':'Tick the square to select this hypothesis for activation'}</b>${segment.summary?`<p>${esc(segment.summary)}</p>`:''}<small><b>${observationLabel}:</b> ${(segment.traits||[]).slice(0,5).map(esc).join(' · ')}</small></div></label>`;}).join('')+(listId?`<div class="reference-selection-action"><div><strong>${active?'Customer model active in Discovery':'Select a profile, then activate'}</strong><small>${active?'This selected customer context will guide your next company search.':'The checkbox selects the hypothesis. Activate makes it available to the next Company Discovery search.'}</small></div>${active?'':`<button class="primary-btn" type="button" data-activate-reference-list="${esc(listId)}" data-activate-reviewed-profile disabled>Activate selected profile →</button>`}</div>`:'');
  }
  function renderOpportunityMap(state,reference){
    const node=document.getElementById('customer-opportunity-map');if(!node)return;
    const analyzed=reference.rows.filter(row=>reference.analyses?.[row.id]);if(analyzed.length!==1){node.innerHTML='';return;}
    const row=analyzed[0],analysis=reference.analyses[row.id],map=reference.opportunityMap?.customerId===row.id?reference.opportunityMap:null;
    const fresh=Boolean(map?.hypotheses?.length&&map.analysisAt===reference.analyzedAt);
    const markets=(state.targetMarkets||[]).join(', ')||'your selected market';
    node.innerHTML=`<section class="customer-opportunity-map"><span class="eyebrow">Opportunity Map · one customer</span><h3>What does ${esc(row.companyName)} teach us about future buyers?</h3><p>The website describes the customer. Tell LeadIntel why this customer bought from you so it can test that need across ${esc(markets)}. One example is a hypothesis, not a proven customer pattern.</p><form id="customer-opportunity-form"><label>What did you sell?<textarea name="service" required maxlength="600" placeholder="The product or service you delivered">${esc(map?.service||row.productService||'')}</textarea></label><label>What problem did it solve?<textarea name="problem" required maxlength="900" placeholder="The customer's problem or desired outcome">${esc(map?.problem||row.reason||'')}</textarea></label><label>What prompted the deal? <span>(optional)</span><textarea name="dealTrigger" maxlength="600" placeholder="An event, deadline, expansion or change you know about">${esc(map?.dealTrigger||'')}</textarea></label><button class="primary-btn" type="submit">${map?.hypotheses?.length?'Update Opportunity Map':'Build Opportunity Map'}</button></form><div id="customer-opportunity-status" role="status" aria-live="polite"></div>${fresh?`<div class="opportunity-hypotheses"><h4>Potential buyer niches · hypotheses to verify</h4>${map.hypotheses.map(item=>`<article><strong>${esc(item.niche)}</strong><p><b>Shared need:</b> ${esc(item.sharedNeed)}</p><p>${esc(item.whyRelevant)}</p><small><b>Check for:</b> ${esc(item.evidenceToCheck)}</small></article>`).join('')}</div>`:map?.hypotheses?.length?'<p class="opportunity-map-stale">The customer analysis changed. Update the map before using these hypotheses.</p>':''}<div class="opportunity-market-sizing"><strong>How large is this opportunity in ${esc(markets)}?</strong><p>Company count and market value have not been verified. LeadIntel needs a defined niche, a sourced company count, and an evidenced spending range before showing an estimate. Company Discovery will verify named prospects and demand signals.</p><button class="secondary-btn" type="button" data-research-opportunity-map>Research companies and signals →</button></div></section>`;
  }
  async function buildOpportunityMap(event){
    event.preventDefault();const form=event.target,button=form.querySelector('[type="submit"]'),status=document.getElementById('customer-opportunity-status');if(button.disabled)return;
    const state=ensureState(readState()),reference=state.referenceCustomers,rows=reference.rows.filter(row=>reference.analyses?.[row.id]);if(rows.length!==1)return;
    const service=clean(form.elements.service.value),problem=clean(form.elements.problem.value),dealTrigger=clean(form.elements.dealTrigger.value);if(!service||!problem)return;
    const workspace=root.LeadIntelServerBridge?.workspace?.id;button.disabled=true;button.textContent='Analyzing opportunity niches…';status.textContent='Comparing the customer with your deal context…';
    try{
      const hypotheses=await root.LeadIntelReferenceCustomerAI.requestOpportunityMap({workspaceId:workspace,customer:rows[0],analysis:reference.analyses[rows[0].id],seller:{website:state.website,priorityOffers:state.profile?.priorityOffers},service,problem,dealTrigger,market:(state.targetMarkets||[]).join(', ')});
      const current=ensureState(readState());if(!current.referenceCustomers.rows.some(row=>row.id===rows[0].id)||!current.referenceCustomers.analyses?.[rows[0].id]||current.referenceCustomers.analyzedAt!==reference.analyzedAt)throw new Error('Customer analysis changed. Review it and build the map again.');
      current.referenceCustomers.opportunityMap={customerId:rows[0].id,service,problem,dealTrigger,hypotheses,updatedAt:new Date().toISOString(),analysisAt:current.referenceCustomers.analyzedAt};
      const Portfolio=root.LeadIntelReferenceCustomerPortfolio;const saved=Portfolio?.syncCurrentList?.(current)||current;
      await writeState(saved);render();root.LeadIntelReferenceCustomerLibraryUI?.sync?.();
    }catch(error){status.textContent=error?.message||'Opportunity analysis failed. Your customer list is unchanged.';button.disabled=false;button.textContent='Build Opportunity Map';}
  }
  function mergeRows(state,newRows,source){
    const existing=state.referenceCustomers?.rows||[];const combined=Ref.normalizeImportedRows([...existing.map(row=>({Company:row.companyName,Website:row.website})),...newRows.map(row=>({Company:row.companyName,Website:row.website}))],{sourceType:'state'});
    const incomingByKey=new Map(newRows.map(row=>[row.domain||`${row.companyName.toLowerCase()}`,row]));
    const preservedAnalyses=state.referenceCustomers?.analyses||{};
    state.referenceCustomers=Ref.normalizeReferenceState({...state.referenceCustomers,source,rows:combined.map(row=>{const incoming=incomingByKey.get(row.domain||row.companyName.toLowerCase());return incoming?{...row,status:incoming.status,reviewed:incoming.reviewed}:row;}),analyses:preservedAnalyses,segments:[],activeSegmentIds:[],activeIds:[],activated:false,dna:null});return state;
  }
  async function handleFile(file){if(!file)return;const modal=ensureModal(),statusNode=modal.querySelector('#reference-import-status');statusNode.textContent='Reading customer list…';try{
      const name=file.name||'customer-list',ext=(name.split('.').pop()||'').toLowerCase();let rawRows=[],sourceType=ext;
      if(ext==='csv'){rawRows=Ref.parseCsv(await file.text());}
      else if(ext==='xlsx'||ext==='xls'){const XLSX=await import(`https://cdn.jsdelivr.net/npm/xlsx@${XLSX_VERSION}/+esm`);const workbook=XLSX.read(await file.arrayBuffer(),{type:'array'});const sheet=workbook.Sheets[workbook.SheetNames[0]];rawRows=XLSX.utils.sheet_to_json(sheet,{defval:''});sourceType='xlsx';}
      else if(ext==='pdf'){rawRows=await parsePdfRows(file);sourceType='pdf';}
      else throw new Error('Use CSV or Excel. PDF is available as a fallback.');
      const rows=Ref.normalizeImportedRows(rawRows,{sourceType});if(!rows.length)throw new Error('No records found. Include a Company Name, Website, or both.');const state=ensureState(readState());mergeRows(state,rows,{type:sourceType,name});await writeState(state);statusNode.textContent=`${rows.length} companies imported. ${rows.filter(row=>row.status==='ready').length} ready for analysis.`;render();
    }catch(error){statusNode.textContent=error.message||'Unable to import customer list';}}
  async function getPdfModule(){if(pdfModule)return pdfModule;pdfModule=await import(`https://cdn.jsdelivr.net/npm/pdfjs-dist@${PDFJS_VERSION}/build/pdf.min.mjs`);pdfModule.GlobalWorkerOptions.workerSrc=`https://cdn.jsdelivr.net/npm/pdfjs-dist@${PDFJS_VERSION}/build/pdf.worker.min.mjs`;return pdfModule;}
  async function parsePdfRows(file){const pdfjs=await getPdfModule(),pdf=await pdfjs.getDocument({data:await file.arrayBuffer()}).promise;const rows=[];for(let pageNo=1;pageNo<=Math.min(pdf.numPages,30)&&rows.length<200;pageNo++){const page=await pdf.getPage(pageNo),content=await page.getTextContent();const line=content.items.map(item=>item.str).join(' ').replace(/\s+/g,' ').trim();for(const part of line.split(/\s{2,}|\s*[|•]\s*/)){const value=clean(part);if(value.length<2)continue;const urlMatch=value.match(/https?:\/\/\S+|\b(?:www\.)?[a-z0-9.-]+\.[a-z]{2,}\b/i);if(urlMatch){const website=urlMatch[0];const company=clean(value.replace(urlMatch[0],'').replace(/[-–—,:;]+$/,''))||website;rows.push({Company:company,Website:website});}}}return rows;}
  async function addManual(event){
    event?.preventDefault?.();
    const modal=ensureModal(),form=modal.querySelector('#reference-manual-form'),button=modal.querySelector('#reference-save-manual'),status=modal.querySelector('#reference-import-status');
    const raw={Company:modal.querySelector('#reference-manual-company').value,Website:modal.querySelector('#reference-manual-website').value};
    const rows=Ref.normalizeImportedRows([raw],{sourceType:'manual'});
    if(!rows.length){status.textContent='Enter a company name, a website, or both.';modal.querySelector('#reference-manual-company')?.focus();return;}
    if(button.disabled)return;
    button.disabled=true;button.textContent='Adding…';
    try{
      const state=ensureState(readState()),incoming=rows[0],existing=state.referenceCustomers.rows.some(row=>(incoming.domain&&row.domain===incoming.domain)||row.id===incoming.id);
      if(existing){status.textContent=`${incoming.companyName||incoming.domain} is already in this list.`;return;}
      mergeRows(state,rows,{type:'manual',name:'Manual entry'});
      writeState(state);
      form.hidden=true;modal.querySelector('#reference-add-manual')?.setAttribute('aria-expanded','false');
      for(const id of ['reference-manual-company','reference-manual-website'])modal.querySelector(`#${id}`).value='';
      render();
      root.LeadIntelReferenceCustomerLibraryUI?.openEditor?.();
      status.textContent=state.referenceCustomerPortfolio?.selectedListId
        ?`${incoming.companyName||incoming.domain} added to the draft. Press Save & Analyze to save the change and review its buyer context.`
        :`${incoming.companyName||incoming.domain} added. Press Save & Analyze to keep the list and start research.`;
    }catch(error){
      status.textContent=error?.message||'Unable to add the company. Your current list is still available.';
      form.hidden=false;modal.querySelector('#reference-add-manual')?.setAttribute('aria-expanded','true');
    }finally{button.disabled=false;button.textContent='Add company';}
  }
  async function confirmRow(id){const state=ensureState(readState());const row=state.referenceCustomers.rows.find(item=>item.id===id);if(!row)return;if(!row.domain){toast('A valid Website is required before analysis');return;}row.status='ready';row.reviewed=true;state.referenceCustomers=Ref.normalizeReferenceState(state.referenceCustomers);await writeState(state);render();}
  async function removeRow(id){const state=ensureState(readState());state.referenceCustomers.rows=state.referenceCustomers.rows.filter(row=>row.id!==id);delete state.referenceCustomers.analyses?.[id];state.referenceCustomers.segments=[];state.referenceCustomers.activeSegmentIds=[];state.referenceCustomers.activeIds=[];state.referenceCustomers.activated=false;state.referenceCustomers.dna=null;state.referenceCustomers=Ref.normalizeReferenceState(state.referenceCustomers);await writeState(state);render();}
  function inferAnalysis(text=''){const hay=clean(text).toLowerCase();const industry=/manufactur|factory|production|ražošan|rūpnīc/.test(hay)?'industrial manufacturing':/logistics|warehouse|noliktav/.test(hay)?'logistics':/software|saas|platform|technology/.test(hay)?'software / technology':/construction|būvniec/.test(hay)?'construction':/consult|training|coaching|professional service/.test(hay)?'professional services':'';const employees=hay.match(/(?:about|over|more than|approximately|around)?\s*(\d{2,5})\s+(?:employees|staff|people|darbiniek)/i);const n=employees?Number(employees[1]):0;const sizeBand=n?(n<50?'<50':n<=100?'50-100':n<=500?'100-500':'500+') :'';const businessModel=/\bb2b\b|business customers|corporate clients|industrial clients|companies/.test(hay)?'B2B':'';const growthStage=/expansion|growing|new market|export|investment|paplašin|eksport|investīc/.test(hay)?'growth / expansion':'';const operatingComplexity=/multiple locations|multi-site|factory|warehouse|operations|production/.test(hay)?'operationally complex':'';const buyerRoles=[];for(const [label,re] of [['CEO',/chief executive|\bceo\b/],['Sales Director',/sales director|head of sales/],['HR Director',/hr director|human resources director/],['COO',/chief operating officer|\bcoo\b/],['Procurement',/procurement|purchasing|iepirkum/],['Operations',/operations director|operations manager/],['Plant Manager',/plant manager|production manager/]])if(re.test(hay))buyerRoles.push(label);const buyingTriggers=[];for(const [label,re] of [['Expansion',/expansion|new facility|new factory|capacity|paplašin/],['Modernization',/moderni[sz]|automation investment|equipment upgrade|digital transformation/],['Hiring',/hiring|recruit|vacanc/],['Market entry',/new market|market entry|export expansion/]])if(re.test(hay))buyingTriggers.push(label);const customerOutcome=/increase sales|sales performance|revenue growth|conversion/.test(hay)?'improve commercial performance':/efficien|productivity|reduce cost/.test(hay)?'improve operational efficiency':'';const supported=[industry,sizeBand,businessModel,growthStage,operatingComplexity,customerOutcome,...buyerRoles,...buyingTriggers].filter(Boolean).length;return {industry,sizeBand,businessModel,growthStage,operatingComplexity,customerOutcome,buyerRoles,buyingTriggers,confidence:supported>=5?'high':supported>=2?'medium':'low'};}
  async function analyzeRow(row){const response=await fetch(`${FIRECRAWL_PROXY}/firecrawl-scrape`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url:row.website,formats:['markdown'],onlyMainContent:true,timeout:25000})});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||`Source returned ${response.status}`);const data=payload.data||payload;const text=String(data.markdown||data.content||'').slice(0,25000);if(!text.trim())throw new Error('No readable website content');return inferAnalysis(text);}
  async function analyzeCustomerList(){let state=ensureState(readState());const ready=state.referenceCustomers.rows.filter(row=>row.status==='ready'&&row.website).slice(0,MAX_REFERENCE_ANALYSIS);if(!ready.length){toast('Upload companies with valid websites first');return;}const modal=ensureModal(),statusNode=modal.querySelector('#reference-import-status'),button=modal.querySelector('#reference-analyze');button.disabled=true;const analyses={...state.referenceCustomers.analyses};let completed=0,failed=0;for(const row of ready){statusNode.textContent=`Analyzing customer websites… ${completed+1}/${ready.length}`;try{analyses[row.id]=await analyzeRow(row);}catch{analyses[row.id]={confidence:'low'};failed++;}completed++;}state=ensureState(readState());state.referenceCustomers.analyses=analyses;const segmentation=Ref.buildReferenceSegments(state.referenceCustomers.rows,analyses);state.referenceCustomers.segments=segmentation.segments;state.referenceCustomers.segmentationMeaningful=segmentation.meaningful;state.referenceCustomers.activeSegmentIds=[];state.referenceCustomers.activeIds=[];state.referenceCustomers.activated=false;state.referenceCustomers.dna=null;state.referenceCustomers.analyzedAt=new Date().toISOString();state.referenceCustomers=Ref.normalizeReferenceState(state.referenceCustomers);await writeState(state);statusNode.textContent=`Analysis complete · ${ready.length-failed}/${ready.length} websites produced usable intelligence${failed?` · ${failed} need review`:''}. Review the Customer segments below before activation.`;button.disabled=false;render();}
  async function activateSelectedSegments(){let state=ensureState(readState());const segmentIds=selectedSegmentIds();if(!segmentIds.length){toast('Select at least one customer segment');return;}state.referenceCustomers=Ref.activateReferenceSegments(state.referenceCustomers,segmentIds);state.referenceCustomers.dna=Ref.buildReferenceDna(state.referenceCustomers,state.referenceCustomers.analyses);state.referenceCustomers.analyzedAt=new Date().toISOString();await writeState(state);render();toast('Reference Customer priority model activated');}
  root.LeadIntelReferenceCustomerUI={open,close,render};
})(globalThis);
